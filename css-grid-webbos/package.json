{
  "name": "application-name",
  "version": "0.0.1",
  "scripts": {
    "start": "browser-sync start --server --files '**/*.css,**/*.html,**/*.js' --directory --port 7777 --browser 'chrome'"
  },
  "devDependencies": {
    "browser-sync": "^2.18.13"
  }
}
